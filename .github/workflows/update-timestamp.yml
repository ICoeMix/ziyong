name: Update File Timestamps

on:
  push:
    branches: [ main ]

jobs:
  update-timestamps:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ 拉取完整仓库历史
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ 查找文件
      - name: Find files with timestamp placeholder
        id: find_files
        run: |
          FILES=$(grep -rl --include=\*.{js,ts} '⟦TIMESTAMP⟧' . || true)
          echo "files=$FILES" >> $GITHUB_OUTPUT

      # 3️⃣ 替换占位符
      - name: Update timestamps
        run: |
          FILES="${{ steps.find_files.outputs.files }}"
          if [ -z "$FILES" ]; then
            echo "No files with timestamp placeholder found, skip."
            exit 0
          fi

          echo "$FILES" | tr ' ' '\n' | while IFS= read -r FILE; do
            if [ ! -f "$FILE" ]; then
              echo "File not found: $FILE, skip."
              continue
            fi

            # 获取文件最后修改时间
            LAST_MOD=$(git log -1 --format=%cd --date=format:'%Y-%m-%d %H:%M' -- "$FILE")

            # 如果 git log 为空（首次提交或文件未修改），使用当前时间
            if [ -z "$LAST_MOD" ]; then
              LAST_MOD=$(date +"%Y-%m-%d %H:%M")
            fi

            TIMESTAMP="⟦$LAST_MOD⟧"

            # 替换占位符
            sed -i "s/⟦TIMESTAMP⟧/$TIMESTAMP/g" "$FILE"
            git add "$FILE"
            echo "Updated $FILE -> $TIMESTAMP"
          done

      # 4️⃣ 提交修改
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "🕒 Auto update file timestamps"
            BRANCH=$(git rev-parse --abbrev-ref HEAD)
            git push origin "$BRANCH" || echo "Push failed, maybe branch protected"
