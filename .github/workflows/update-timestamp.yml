name: Update Timestamp

on:
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Check if target file changed
        id: check
        run: |
          # 检查 push 里这个文件是否有修改
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -q "^script.js$"; then
            echo "file_changed=true" >> $GITHUB_OUTPUT
          else
            echo "file_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Update timestamp if file changed
        if: steps.check.outputs.file_changed == 'true'
        run: |
          TIMESTAMP=$(date +"⟦%Y-%m-%d %H:%M⟧")
          echo "当前时间: $TIMESTAMP"
          sed -i "s/资源解析器 ©𝐒𝐡𝐚𝐰𝐧\s*⟦[0-9:\- ]*⟧/资源解析器 ©𝐒𝐡𝐚𝐰𝐧  $TIMESTAMP/g" script.js
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add script.js
          git commit -m "🕒 Auto update timestamp"
          git push
